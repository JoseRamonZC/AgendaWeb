{% extends 'base.html.twig' %}

{% block title %}{{ parent() }} - Dashboard
{% endblock %}

{% block body %}
<nav class="navbar navbar-expand-lg navbar-light">
          <div class="container-fluid">
            <a class="navbar-brand mr-4" href="{{ path('app_dashboard') }}">Agenda Web</a>
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
              <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarSupportedContent">
              <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item ml-3">
                {% if is_granted('IS_AUTHENTICATED_REMEMBERED') %}
                {% if app.request.get('_route') == "app_evento_new" %}
                  {% else %}
                  {% endif %}
                </li>
                <li class="nav-item">
                {% if app.request.get('_route') == "app_evento" %}
                  <a class="nav-link a ml-3 mr-5 active" href="{{ path('app_evento') }}">Mis eventos</a>
                  {% else %}
                  <a class="nav-link a ml-3 mr-3" href="{{ path('app_evento') }}">Mis eventos</a>
                  {% endif %}
                </li>

                {# Comprobamos que el usuario este registrado #}

                  
                    
                    <a href="{{ path('app_logout') }}"><button class="boton-nav a">Logout</button></a>
                  {% else %}
                    <a href="{{ path('app_login') }}"><button class="boton-nav a mr-3">Login</button></a>
                    <a href="{{ path('app_registro') }}"><button class="boton-nav a mr-3">Registrate</button></a>
                {% endif %}
            </div>
          </div>
        </nav> 
          <script src="{{ asset('js/jquery.min.js') }}"></script>
          <script src="{{ asset('bootstrap/js/bootstrap.js') }}"></script>
  </head>
    
    <script>

      document.addEventListener('DOMContentLoaded', function() {
        var calendarEl = document.getElementById('calendar');
        var calendar = new FullCalendar.Calendar(calendarEl, {
          initialView: 'dayGridMonth',
          dateClick: function(info) {
            alert('Ha seleccionado: ' + info.dateStr);
            alert('Vista actual: ' + info.view.type);
            // change the day's background color just for fun
            info.dayEl.style.backgroundColor = 'red';
            $("#exampleModal").modal();
          },
          locale:"Es",
          headerToolbar:{
            left:'prev,next, today',
            center:'title',
            right:'dayGridMonth,timeGridWeek,timeGridDay'
          },
           eventSources: [

            // your event source
            {
              events: [ // put the array in the `events` property
                {
                  title  : 'event1',
                  description : 'estamos solos',
                  start  : '2022-10-01'
                },
                {
                  title  : 'event2',
                  description : 'estamos solos2',
                  start  : '2022-10-05',
                  end    : '2022-10-07'
                },
                {
                  title  : 'event3',
                  description : 'estamos solos3',
                  start  : '2022-10-09T12:30:00',
                }
              ],
              color: 'black',     // an option!
              textColor: 'yellow' // an option!
            }],

            eventClick:function(callEvent, jsEvent, view){
              $("#tituloEvento").html(calllEvent.title);
              $("#descripcionEvento").html(calllEvent.descripcion);

              $("#exampleModal").modal();
            }

        });
        calendar.render();
      });

    </script>

    
  <body>

<!-- Modal -->
<div class="modal fade" id="exampleModal" tabindex="-1" aria-labelledby="exampleModalLabel" aria-hidden="true">
  <div class="modal-dialog">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title" id="tituloEvento"></h5>
        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
      </div>
      <div class="modal-body">
        <div id ="descripcionEvento"></div>
      </div>
      <div class="modal-footer">
        <a href="{{ path('app_evento_new') }}"><button id="boton" class="btn-success" data-bs-dismiss="modal">Añadir</button></a>
        <a href="{{ path('app_evento_edit') }}"><button id="boton" class="btn-success" data-bs-dismiss="modal">Modificar</button></a>
        <a href="{{ path('app_evento_delete') }}"><button id="boton" class="btn-danger" data-bs-dismiss="modal">Borrar</button></a>
        <a href="{{ path('app_dashboard') }}"><button id="boton" class="btn-secondary" data-bs-dismiss="modal">Cerrar</button></a>
        {# {{ include('evento/_delete_form.html.twig') }} #}
      </div>
    </div>
  </div>
</div>

    <div class="p-5" id='calendar'></div>
  </body>
</html>
{% endblock %}
